# Local Edge Function defaults. Tracked on purpose: it holds no credential, because local
# invitation delivery is captured instead of sent. Any other environment must configure a
# real provider or `send-invitation` refuses to send. See docs/security/database-security.md.
INVITATION_ENV=local
INVITATION_SITE_URL=http://127.0.0.1:3000
